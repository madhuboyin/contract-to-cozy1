# Ask Cozy Cross-Domain Interaction Rollout — Phase 6 Buyer Acceptance Scenario Verification

**Status:** Complete — all 10 acceptance scenarios (B01–B10) plus the Phase 6 exit criterion's "no undeclared item mutation" clause evaluated. **B06 revised 2026-09-17 after external review found the same "generic conflict message ≠ current state shown" defect already corrected in Phase 8's P04 and Phase 7's D07. B04 fixed across 2 rounds: round 1 (old/new-date disclosure + editableFields + single-target reconciliation) was re-scored FULL PASS→PARTIAL after external review found the reconciliation half only refreshed the single list the mutation was launched from; round 2, per an explicit user-scoped design decision, added a server-owned mutation-impact map (`ASK_MUTATION_IMPACT_MAP`) that also refreshes `BUYER_PLAN_STATUS`/`BUYER_DEADLINES`/conditionally `BUYER_MOVE_STATUS`, plus a platform-wide confirm-replay fix, closing the gap back to PASS. B03 FIXED 2026-09-17 by reusing that same mechanism — `BUYER_TASK_COMPLETE` added to the impact map with the identical sibling set. B07 re-traced 2026-09-17 and found far narrower than originally characterized (most of the mechanism was already generic in the frontend, not Maintenance-exclusive); a scoped fix generalized `reconcileResultView` and added a selection marker/highlight to the generic renderer — the first frontend code change in this engagement — closing FAIL→PARTIAL. B02 FIXED 2026-09-17, per an explicitly scoped user design decision with 2 rounds of correction during scoping (same-result-identity needed `BUYER_DEADLINES` added to `FILTER_CONTINUABLE_OPERATIONS` plus a real `AskViewState`; full-scope counts needed a new pre-cap `overview.blockersByLane` field, not the legacy 5-item-capped `blockers` field) — 4-lane filtering for blockers, milestones left honestly unfiltered pending a canonical phase mapping, closing FAIL→PARTIAL. B01 and B10 FIXED 2026-09-17 together: a new `buyerJourneyStageLabel` surfaces the current `BuyerJourneyStage` in `buyerPlanStatusResult`'s SUMMARY body (B01, PARTIAL→PASS); `buyerPlanStatusResult`/`buyerCostReadinessResult` now suppress suggestions when genuinely nothing is open/costed rather than an unconditional static pair (B10, scoped to these 2 operations, FAIL→PASS). B06 FIXED 2026-09-17: new `buyerTaskConflictDescription`/`buyerFindingConflictDescription` functions, mirroring `maintenanceConflictDescription`'s own reference shape exactly, replace the static conflict message across all 3 Buyer confirm handlers with one that interpolates real current-record state (title/status/priority/due date, or current disposition for findings) — closing PARTIAL→PASS. B02's milestone-filtering gap and B07's filter-restoration gap BOTH CLOSED 2026-09-17, per explicit user approval of a canonical `BUYER_MILESTONE_TYPE_LANE` mapping (all 18 `BuyerMilestoneType` values, 4 explicitly lane-less) and explicit instruction to verify (not build new filters for) `BUYER_DEADLINES`'s existing mechanism — B02 PARTIAL→PASS, B07 PARTIAL→PASS.**
**Verdict: Phase 6's exit criterion ("B01–B10 pass and the buyer operation family has no undeclared item mutation") is now MET.** Originally scored 4 of 10 fully passing; after every fix and correction to date, all 10 of 10 scenarios now pass fully, 0 partial, 0 fail. The mutation-discipline half of the exit criterion also passes.
**Governs:** [Ask Cozy — Cross-Domain Interaction Rollout FRD](../product/ASK_COZY_CROSS_DOMAIN_INTERACTION_ROLLOUT_FRD.md) §10.3 (B01–B10) and §21 Phase 6's exit criterion.
**Builds on:** [Phase 0 Coverage Audit](ASK_COZY_PHASE0_COVERAGE_AUDIT.md) §4.10, which traced all 18 Buyer operations to the handler level. This document re-reads the code against each specific acceptance scenario rather than re-deriving the underlying evidence from scratch.
**Verification level: STATIC.** Every verdict below is derived from reading `askOrchestrator.service.ts`'s buyer handlers, `askDomainCommandRegistry.ts`, `HomeBuyerTask.service.ts`, and `buyerPurchaseLenderReadiness.service.ts` directly, plus the `tsc`-clean, test-green state confirmed in the Phase 0 audit. B07's fix additionally touches the frontend (`AskWorkspace.tsx`, `resultViewState.ts`) — verified via `tsc --noEmit` and Jest, same STATIC boundary, not a live browser session. Nothing here is database- or browser-verified — no live Buyer Plan was exercised end to end.

| Field | Status |
| --- | --- |
| Audit coverage | Complete — all 10 acceptance scenarios (B01–B10) + mutation-discipline clause evaluated |
| Static (code-read) acceptance | 10 pass, 0 partial, 0 fail (scenarios); mutation-discipline clause passes separately |
| Runtime/browser acceptance | Not evaluated this session — no live database or browser exercised |
| Phase 6 exit criterion | **Met** — all 10 of 10 scenarios pass |

## Methodology

Each scenario's "required outcome" is quoted verbatim from FRD §10.3. Verdicts:
- **PASS** — the code does what the scenario requires, confirmed by direct reading.
- **PARTIAL** — part of the required outcome is met; a specific, named part is not.
- **FAIL** — the required outcome is not met, confirmed by direct reading (not by absence of evidence).
- Scoping notes are added where the scenario doesn't map to a single, unambiguous Buyer operation.

## B01 — Ask for current buyer-plan status

**Required:** "One scoped progress surface with current phase, blockers, counts and freshness."

**Original verdict: PARTIAL.**

`buyerPlanStatusResult` (`askOrchestrator.service.ts:4599`) surfaces: progress percent/completed/total (`overview.journey.progress`), blocker count (`overview.blockers.length`), the exact next task, and freshness (`data.contextVersion`, a hash of the whole presentation payload). That's a real, scoped, single surface — not a list of disconnected facts.

**What was missing:** "current phase" was not rendered anywhere in the result. The plan's real `overview.journey.stage` field (a 9-value `BuyerJourneyStage`: `EXPLORING`/`OFFER_CONTRACT`/`DUE_DILIGENCE`/`CLOSING_PREP`/`CLOSED`/`MOVE_IN`/`FIRST_30_DAYS`/`DAYS_31_TO_90`/`HANDED_OFF`) was already computed and returned to Ask, but `buyerPlanStatusResult` never read or surfaced it. A homeowner got "68% complete, 2 blockers" but never "You're in Due Diligence." Confirmed by direct grep: zero references to `stage`/`phase` anywhere in the handler.

**FIXED, same session, 2026-09-17: `PASS`.** The SUMMARY block's `body` now opens with "You're in the [Phase] phase" before the existing progress/blocker sentence. A new `buyerJourneyStageLabel` (pure, exported for direct testing) maps all 9 `BuyerJourneyStage` values to homeowner-facing labels — checked first for an existing canonical label helper for this exact enum (none found: `HomeBuyerTask.service.ts`'s own `currentBuyerPhase(stage)` is a narrower, lossy 4-way mapping built for task-ranking, not display, and the frontend's `RecentOwnerTransition.tsx` `STAGE_LABELS` only covers the 4 post-close values on a different, narrower type) — falls back to a humanized raw enum value for any future stage this map doesn't yet name explicitly, degrading gracefully rather than throwing or omitting the phase.

**Verification: 2 new unit tests** in `tests/ask/buyerPlanStatusPhaseAndSuggestions.test.js` covering `buyerJourneyStageLabel` directly (all 9 declared stage values map to their own label; an unrecognized value falls back to a humanized string) — all pass, alongside a clean `tsc --noEmit` and fresh-parse check. **Not** database- or browser-verified — the actual rendered sentence was not viewed in a live browser this session.

## B02 — Filter deadlines and refine by phase

**Required:** "Same result identity; full-scope counts; no duplicate list."

**Original verdict: FAIL.**

There was no phase-based filtering mechanism anywhere in `buyerDeadlinesResult` (`askOrchestrator.service.ts:4633`) — confirmed by direct grep for `stage`/`phase` in the function body (zero matches). The operation returned milestones and blockers unconditionally; its `GROUPED_LIST` block declared `filters: []`, the same empty-filters pattern found across every Buyer read in the Phase 0 audit (§4.10). There was no declared filter chip, no message-text phase parsing, and no "same result identity across a refinement" to test, because refinement by phase wasn't implemented at all.

**FIXED, same session, 2026-09-17, per an explicitly scoped user design decision** (2 rounds of correction during scoping — see below): `buyerDeadlinesResult` now supports filtering its "Blocking before closing" section by one of the 4 existing readiness lanes (CONTRACT/DUE_DILIGENCE/CLOSING/MOVE — the same grouping already exposed as `overview.readinessLanes` and already used by `BUYER_MOVE_STATUS`'s own `?filter=MOVE` href), not the raw 8-value `BuyerPlanPhase` enum. A new pure `parseBuyerDeadlineLaneFilter` reuses `CLOSING_HOME_LANES`' own label text directly (exported from `HomeBuyerTask.service.ts` for reuse) rather than a second, independently-maintained regex set, so chip-generation and parsing can never drift apart. The `GROUPED_LIST` block now declares 5 real filter chips (All + the 4 lanes); milestones (which have no phase field at all — confirmed by direct read of their DTO) stay unfiltered but are relabeled "Purchase-wide milestones — not affected by this task-phase filter" whenever a lane filter is active, disclosing the scope boundary rather than silently implying they were filtered too.

**Correction 1 (same-result-identity):** the first draft of this fix used stateless, on-demand filtering alone. User correction: `BUYER_DEADLINES` was absent from `askFollowUpContext.ts`'s `FILTER_CONTINUABLE_OPERATIONS` set, so a declared chip click would not have been recognized as continuing the same operation — it would have routed through ordinary classification instead, not guaranteed to land back on `BUYER_DEADLINES`. Fixed by adding it to that set. Separately, `buyerDeadlinesResult` now populates the platform's generic `viewState` (`resultId`/`statusFilter`/`revision`) via a renamed `loadAskViewState` (was `loadMaintenanceViewState` — Maintenance-only in name only; the mechanism and its `AskExecutionResponseSchema.viewState` field were already documented as generic, this is its 2nd real consumer, same rename convention as B04's `refreshMaintenanceSourceExecution` → `refreshAskSourceExecution`), so the `resultId` carries forward and `revision` increments across a lane-filter refinement instead of relying on a plain `executionId` that changes every turn.

**Correction 2 (full-scope counts):** the first draft filtered `overview.blockers`, which is capped to 5 items across ALL lanes before Ask ever receives it (`HomeBuyerTask.service.ts`'s `getClosingHomePresentation`). User correction: filtering and counting that already-truncated array would understate "how many blocking tasks match this lane" whenever the top-5 cap had already excluded some of that lane's own items. Fixed by adding a new, separate `overview.blockersByLane` field (`BuyerClosingHomeBlockersByLaneEntrySchema` in `buyerAcquisition.contract.ts`) computed from the FULL, pre-cap blocker set inside `getClosingHomePresentation` — one entry per lane with its own authoritative `total` and up to 20 candidate items — leaving the legacy `blockers` field's own meaning and cap completely unchanged for every other consumer of this shared presentation contract.

**Verification: 5 new unit tests** in `tests/ask/buyerDeadlineLaneFilter.test.js` (all 4 lane labels recognized case-insensitively; an explicit "All" reset and an ordinary unrelated question both correctly match no lane; a multi-word label like "Closing readiness" doesn't false-positive against the other 3), 1 new test in `tests/ask/askFollowUpContext.test.js` (a declared lane-filter chip continues `BUYER_DEADLINES`, self-sufficient, no concatenation — mirroring Maintenance's own equivalent test). The `AskViewState` rename was verified against all 9 existing call sites plus the full 41-test Maintenance view-state/coverage-matrix/governance suite (all green, confirming the rename didn't regress Maintenance's own filter-chip mechanism). 2 pre-existing test fixtures (`homeBuyerSlice2ClosingHome.test.js`, `askBuyerPlanPresentation.test.js`) needed a `blockersByLane` field added to satisfy the schema's new (additive) required field — an expected consequence of extending a `z.strictObject`, not a design defect. `tsc --noEmit` clean, fresh-parse clean on all 4 touched backend files, full `tests/ask/*.test.js` suite (675/676 green, 1 pre-existing unrelated skip) and the broader `homeBuyerSlice*.test.js` unit suite (95/97 green — the 2 failures are pre-existing, unrelated frontend-component-content-regex checks, confirmed identical via `git stash` on the clean baseline). **Not** database- or browser-verified — a live multi-turn conversation clicking a lane chip against real Buyer Plan data was not exercised end to end.

**Milestone-filtering follow-up FIXED, same session, 2026-09-17, per an explicitly approved user design decision:** a new, exhaustive `BUYER_MILESTONE_TYPE_LANE` mapping (`HomeBuyerTask.service.ts`, right after `CLOSING_HOME_LANES`) maps all 18 `BuyerMilestoneType` values to one of the 4 lanes, with `DAY_30`/`DAY_60`/`DAY_90`/`CUSTOM` explicitly mapped to no lane (genuinely post-closing or arbitrary, not simply unhandled). `getClosingHomePresentation` now computes `overview.milestonesByLane` (one entry per lane, `total`/`items` from the full, pre-cap milestone set — the same double-truncation fix already applied to blockers in Correction 2, proactively applied here to close the identical defect class) and `overview.unmappedMilestones` (the 4 lane-less types). `buyerDeadlinesResult` now calls a new pure `selectBuyerDeadlineMilestones` to split milestones into lane-matching and unmapped sets; unmapped milestones surface in their own always-shown "Purchase-wide milestones — not affected by this filter" section rather than being silently dropped or mis-attributed to a lane. Per the user's explicit answer during scoping ("Approve mapping; unmapped milestones always show"), unmapped milestones are never excluded by an active lane filter.

**Revised verdict: `PASS`.** Same result identity, full-scope counts, and no-duplicate-list are real for blockers (Correction 1/2, above); milestones now filter by phase via the same canonical, exhaustive mapping, with the 4 genuinely lane-less types disclosed in their own labeled section instead of either silently included in every lane or silently dropped. "Filter deadlines by phase" now applies to both blockers and milestones. Verified by 8 new unit tests in `tests/ask/buyerDeadlineLaneFilter.test.js`: an exhaustiveness test asserting every `BuyerMilestoneType` key maps to exactly one of the 4 lanes or explicitly to `null` (catches a future enum addition silently falling through undecided), plus 3 tests for `selectBuyerDeadlineMilestones` (unfiltered legacy list minus completed when no lane is active; lane-scoped matching plus separately-surfaced unmapped when a lane is active; the zero-match-in-lane case still surfaces the unmapped set rather than an empty result). 2 pre-existing fixtures (`homeBuyerSlice2ClosingHome.test.js`, `askBuyerPlanPresentation.test.js`) needed `milestonesByLane`/`unmappedMilestones` added to satisfy the schema's new additive required fields — same expected consequence as Correction 2's `blockersByLane` fixture updates. `tsc --noEmit` clean, fresh-parse clean on all 3 touched backend files. **Not** database- or browser-verified.

## B03 — Complete a buyer task

**Required:** "Exact task review/confirmation, one write, receipt and plan reconciliation."

**Original verdict: PARTIAL.**

- *Exact task review/confirmation*: real. `buyerTaskCompleteResult` uses `maintenanceCompletionMatch`/exact single-match resolution, and the confirmation card shows task/current-status/actual-cost/recurrence fields before anything changes.
- *One write*: real. The shared `AskConfirmationReceipt` claim/lease/replay mechanism (confirmed platform-wide in the Phase 0 audit) makes a retried confirmation resolve to the already-completed result rather than writing twice.
- *Receipt*: real. `confirmBuyerTaskComplete` returns a `COMPLETED`/`WORKFLOW_PROGRESS` result with the task's real post-completion state.
- *Plan reconciliation*: **originally failed.** Confirmed by direct grep of `confirmBuyerTaskComplete` (Phase 0 audit §4.10): it did not populate `refreshedExecutions`. A still-visible `BUYER_PLAN_STATUS` result showing "2 blockers, 68% complete" did not update after this task was completed — the homeowner would have to re-ask to see the new progress. Maintenance's own `confirmMaintenanceTaskComplete` was the one place in the entire platform this mechanism was actually wired (Phase 0 §4.15); Buyer never adopted it.

**FIXED, same session, 2026-09-17: `PASS`.** Reuses the exact same B04 mechanism (`ASK_MUTATION_IMPACT_MAP` + `reconcileAskExecutionSideEffects`), not a new one — `BUYER_TASK_COMPLETE` was added to the impact map declaring the same `BUYER_PLAN_STATUS`/`BUYER_DEADLINES` siblings `BUYER_TASK_UPDATE` already had, since completing a task changes the same membership/counts a reschedule does. The move-task conditional (adding `BUYER_MOVE_STATUS`) generalizes automatically: `impactedSiblingOperationIds` is keyed on `parameters.buyerTaskId` (a field both `BUYER_TASK_UPDATE` and `BUYER_TASK_COMPLETE` already store identically), not a per-operation branch, so no separate move-task logic needed writing for Complete. `buyerTaskCompleteResult` now threads `sourceExecutionId` through from `envelope.launchContext?.sourceExecutionId`, matching `BUYER_TASK_UPDATE`'s own registration; `confirmBuyerTaskComplete` calls `reconcileAskExecutionSideEffects` after the completion succeeds and pushes the same `LIMITATION` block on a refresh failure. Also benefits from B04's platform-wide confirm-replay fix (a retried completion confirm now still redelivers reconciliation, not just the bare stored result).

**Verification: 1 new unit test** (`tests/ask/buyerMutationImpactReconciliation.test.js`, alongside the 11 already added for B04) confirming `ASK_MUTATION_IMPACT_MAP.BUYER_TASK_COMPLETE` declares the same siblings as `BUYER_TASK_UPDATE`; the shared `siblingOperationIdsForBuyerTaskMutation`/`selectSiblingRefreshTargets`/`capReconciledChildExecutions` pure functions and their existing tests already cover the rest of the mechanism, since it's the identical code path. `tsc --noEmit` clean, fresh-parse clean, full `tests/ask/*.test.js` suite green. **Not** database- or browser-verified — a live completion with a still-open `BUYER_PLAN_STATUS`/`BUYER_DEADLINES` card in the same session was not exercised end to end.

## B04 — Update a deadline that affects another readiness view

**Required:** "Old/new date disclosed; all affected results refreshed or marked stale."

**Original verdict: FAIL.** (Corrected during the original pass — an earlier draft of this section claimed a partial pass by assuming `BUYER_TASK_UPDATE` mirrored `MAINTENANCE_TASK_UPDATE`'s real `editableFields` implementation. Direct re-reading of `buyerTaskUpdateResult`'s exact confirmation block showed it did not.)

- *Old/new date disclosed*: **originally failed.** `buyerTaskUpdateResult`'s confirmation `fields` array was `[{Task}, {Action}, ...(dueDate ? [{'New due date', dueDate}] : []), ...(assignee ? [...] : [])]` — only the **new** date was shown; there was no "Current due date" field, so a homeowner rescheduling a task never saw what it was changing from. `editableFields` was unconditionally `[]`. This was a materially different (weaker) implementation than `MAINTENANCE_TASK_UPDATE`'s own RESCHEDULE handling, which shows both "Current due date" as a read-only field and the new date via a real `editableFields` entry — confirmed by direct comparison of the two functions, not assumed from the pattern looking similar.
- *All affected results refreshed or marked stale*: **originally failed**, same confirmed gap as B03 — `confirmBuyerTaskUpdate` did not populate `refreshedExecutions`, so no other Buyer result (e.g. `BUYER_DEADLINES`, which the rescheduled task may appear in) was refreshed or marked stale after the reschedule succeeded.

**FIXED, same session, 2026-09-17: `PARTIAL` (re-scored from an initial `PASS` after external review — see below).** Both halves now genuinely mirror `MAINTENANCE_TASK_UPDATE`'s own reference implementation rather than approximating it; the reconciliation half's own scope is narrower than "all affected results," matching Maintenance's own known limitation rather than exceeding it.

*Old/new date disclosed*: `buyerTaskUpdateResult`'s confirmation now shows `{ label: 'Current due date', value: humanDate(matched.dueAt) ?? 'Not scheduled' }` as a plain field for RESCHEDULE, with the proposed new date represented only via a real `editableFields` entry (`{ key: 'dueAt', label: 'New due date', type: 'DATE', value: dueDate }`) — not duplicated as read-only text, exactly Maintenance's own CONF-002/CONF-003 shape. Because the frontend's `ConfirmationCard` renders `editableFields` unconditionally for any operation (confirmed by direct read of `AskWorkspace.tsx`, no operation-specific gate), adding this field without also wiring server-side edit support would have rendered a working-looking "Edit" control that failed on every submission — `editAskConfirmation` (`askOrchestrator.service.ts`) was hardcoded to `MAINTENANCE_TASK_UPDATE` only. The fix therefore also generalizes `editAskConfirmation`: the shared access/status/role/version checks stay common, and a new `editBuyerTaskUpdateConfirmation` function (mirroring `MAINTENANCE_TASK_UPDATE`'s own edit path exactly — same optimistic status-and-version-guarded write, same `CONFIRMATION_EDITED` event) now handles `BUYER_TASK_UPDATE`'s reschedule edits against Buyer's own flat parameter shape and `prisma.homeBuyerTask` model. The duplicated yyyy-mm-dd-plus-real-calendar-date validation both edit paths needed is extracted once as `isValidDateEditInput`.

*All affected results refreshed or marked stale*: `confirmBuyerTaskUpdate` now calls the same reconciliation mechanism Maintenance uses, generalized from `refreshMaintenanceSourceExecution` to `refreshAskSourceExecution` (nothing about the original implementation was actually Maintenance-specific — it only read `parameters.sourceExecutionId`) — populating `refreshedExecutions` and, on a refresh failure, a `LIMITATION` block ("Saved; list could not refresh... ask 'What should I do next for this purchase?'"), the same CONF-005 shape Maintenance already had. `buyerTaskUpdateResult` now threads `sourceExecutionId` through from `envelope.launchContext?.sourceExecutionId`, matching `maintenance.update`'s own registration.

**Re-scored FULL PASS → PARTIAL 2026-09-17, external review.** `refreshAskSourceExecution` (`askOrchestrator.service.ts:10016`) reads a single `parameters.sourceExecutionId` string and refreshes at most one prior execution — this closes the specific "the list I clicked this reschedule from is now stale" gap, but "all affected results refreshed or marked stale" is a broader claim than that: another still-open Buyer result in the same conversation that also shows this task (e.g. `BUYER_PLAN_STATUS`, or a second `BUYER_DEADLINES` card the mutation wasn't launched from) is never touched. This is the exact same scope limitation [[project_ask_cozy_cross_domain_rollout_frd|Phase 1's Completed-reference re-verification]] already scores PARTIAL for the identical mechanism on Maintenance ("scoped to exactly one prior execution, not 'each source result whose membership, totals, status or next actions may have changed'") — scoring B04 as a clean PASS while Phase 1 scores the same underlying code PARTIAL was an internal inconsistency (per this series' own standard of scoring one mechanism the same way everywhere it's cited). The old/new-date-disclosure half of B04 remains a clean, unqualified PASS — only the reconciliation half is downgraded.

**FIXED (2nd round), same session, 2026-09-17: `PASS`.** Per an explicit user design decision (scoped in detail before implementation, not inferred), added a server-owned, operation-level "mutation impact" map (`ASK_MUTATION_IMPACT_MAP`, `askOrchestrator.service.ts`) declaring which sibling READ operations a mutation may affect — conservative by design, since XREC-001's own text says "may have changed," not "definitely changed for this exact record," so refreshing an unaffected sibling is a harmless no-op re-render, not a correctness bug. `BUYER_TASK_UPDATE` declares `BUYER_PLAN_STATUS`/`BUYER_DEADLINES` unconditionally, plus `BUYER_MOVE_STATUS` when the specific task being rescheduled is itself `taskType === 'MOVE'` (the same field `buyerMoveStatusResult` already filters on). At confirm time, `refreshImpactedSiblingExecutions` queries same-user/same-session/same-property `AskExecution` rows for those sibling operation ids, restricted to terminal read statuses (excluding `NEEDS_CONFIRMATION`/`RUNNING` — a live confirmation must never be silently re-run out from under the homeowner), excludes any row whose operationId is a registered domain command (defense in depth against ever treating a mutation/proposal as a refreshable read), and keeps only the most-recent row per sibling operation (an older, superseded ask for the same read isn't worth refreshing). Each surviving target is refreshed via the same `refreshAskExecutionAfterConflict` re-run mechanism Maintenance's reference implementation already uses, via `Promise.allSettled` so one sibling's failure can never block the others. `confirmBuyerTaskUpdate` now calls a new `reconcileAskExecutionSideEffects`, which runs both the explicit `sourceExecutionId` refresh and the sibling-impact refresh together and merges their results — capped, via a new `capReconciledChildExecutions`, to `AskExecutionResponseSchema`'s existing `childExecutions` max of 3 (1 explicit source + 3 possible siblings would otherwise exceed it on a move-task reschedule; the explicit source always keeps its slot, siblings fill what's left). Deliberately **not** client-supplied: the server already has the authoritative `userId`/`sessionId`/`propertyId`, and a client-supplied list would still need the server to decide which operations are eligible siblings — a client hint could be added later purely as a performance optimization, but correctness stays server-owned.

**Self-caught replay bug, fixed in the same round:** `confirmAskExecution`'s idempotency-replay branch (`previous.status === 'COMPLETED'`) used to return the stored execution bare, with no `childExecutions` at all — a client that submitted the original confirm, had the mutation genuinely succeed, but never saw the response (a dropped connection) and retried, would get the already-completed result but silently lose every sibling refresh, even though the retry is otherwise fully safe (protected by the same `AskConfirmationReceipt` idempotency this branch already guards). Fixed by calling `reconcileAskExecutionSideEffects` on replay too, using the execution row's own persisted `parametersJson` — this reruns only the read-only reconciliation step and redelivers its result inline; it never replays the mutation itself, which the idempotency receipt was already correctly preventing. This is a platform-wide fix (the branch is shared by every operation, not Buyer-specific) — it also closes the identical, previously-unnoticed gap for Maintenance's own existing single-target reconciliation on a confirm replay.

**Verification: 11 new unit tests** in `tests/ask/buyerMutationImpactReconciliation.test.js` covering the pure, extracted pieces directly: `ASK_MUTATION_IMPACT_MAP`'s declared Buyer siblings; `siblingOperationIdsForBuyerTaskUpdate`'s move-task conditional (adds/doesn't add `BUYER_MOVE_STATUS`); `selectSiblingRefreshTargets`'s no-operationId exclusion, command-operation exclusion, most-recent-per-operation dedup, and empty-input case; `capReconciledChildExecutions`'s cap-of-3 behavior (source always kept, siblings truncated; no-source case; below-cap case). All pass, alongside a clean `tsc --noEmit`, a fresh-parse check, and the full pre-existing Ask suite. The DB-heavy orchestration around these pure pieces (`refreshImpactedSiblingExecutions`'s actual Prisma query, the replay branch's end-to-end behavior against a live idempotency receipt) is **not** independently exercised — same STATIC-verification boundary applied throughout this audit series. **Not** database- or browser-verified — a live multi-card Buyer conversation (reschedule a task with `BUYER_PLAN_STATUS`/`BUYER_DEADLINES`/`BUYER_MOVE_STATUS` all previously asked about in the same session) was not exercised end to end.

**Verification level: 9 new unit tests** — 5 in `tests/ask/dateEditInputValidation.test.js` covering `isValidDateEditInput` directly (well-formed dates, non-string values, malformed shapes, an out-of-range month rejected, and one genuinely surprising finding verified by direct execution before asserting it: JS's `Date` constructor silently rolls a nonexistent day-of-month like `2026-02-30` over into `2026-03-02` rather than producing `NaN` — a pre-existing characteristic of Maintenance's own original validation, extracted here verbatim rather than introduced or silently fixed by this change, and explicitly disclosed rather than asserted away) — plus the 4 already-existing test-count contribution from this session's other extracted helpers in the same suite run. All pass, alongside a clean `tsc --noEmit` and a direct fresh-parse check. **Not** database- or browser-verified — the actual edit-submission round trip (does clicking "Edit" → "Save" on a Buyer reschedule confirmation genuinely persist and re-render correctly) was not exercised against a live database or browser this session.

## B05 — Several findings could match "handle this"

**Required:** "Target clarification before disposition or task creation."

**Verdict: PASS.**

`buyerFindingDispositionResult` (`askOrchestrator.service.ts:5392`) uses `maintenanceCompletionMatch` for entity resolution; when it doesn't resolve to exactly one finding (or the action word is ambiguous), it returns `NEEDS_ENTITY` with a `GROUPED_LIST` of the open findings and an explicit instruction ("Say negotiation, post-close, verified fact, or dismissed") rather than guessing. No disposition or task creation happens on an ambiguous match — confirmed by direct reading, matching ROLL-006 exactly.

## B06 — Contract revision changes during review

**Required:** "Stale proposal blocked; current date/state shown for renewed review."

**Original verdict: PARTIAL — corrected from an original PASS after external review; the same defect independently found in Phase 8's P04 and Phase 7's D07.**

`BUYER_CONTRACT_TIMELINE` itself is a pure read with no domain command — there is no direct "edit the contract" confirmation flow to test this scenario against literally. The underlying mechanism it's really testing — a confirmation rejected because the record changed after the review card was shown — is scoped here to the closest directly-testable analogue: `confirmBuyerTaskUpdate`/`confirmBuyerTaskComplete`/`confirmBuyerFindingDisposition`.

*Stale proposal blocked*: genuinely real. All three carry a real per-entity version (`buyerTaskVersion`, `matched.buyerDispositionAt`) checked against the value captured at propose time, on top of the platform-wide `confirmAskExecution` mechanism (atomic version re-check inside the claim transaction, `ASK_CONTEXT_VERSION_CONFLICT` on drift — confirmed platform-wide in Phase 0 §2.5).

*Current date/state shown*: **originally not satisfied, confirmed by direct re-read.** `confirmBuyerTaskUpdate`'s conflict was thrown as `new Error('This task changed while the confirmation was open. Review its current status and try again.')` — a static string with zero task-specific dynamic content (no title, no current due date, no current status). This was the identical pattern independently found in Phase 8's `confirmClaimTransition` (P04) and Phase 7's `confirmSellerPrepItemDecision` (D07): the shared generic error-catch wrapper renders `details: []` and `actions: []` hardcoded empty regardless of operation, so the message string itself IS the entire disclosure. Only `MAINTENANCE_TASK_COMPLETE`/`UPDATE`'s dynamic `maintenanceConflictDescription` genuinely interpolated real current-record values anywhere in this codebase at the time.

**FIXED, same session, 2026-09-17: `PASS`.** All 3 Buyer conflict sites now throw a dynamic, task/finding-specific message, mirroring `maintenanceConflictDescription`'s own shape exactly (same "already completed"/"cancelled" special cases, same "current status, priority, due date" fallback for anything else): a new `buyerTaskConflictDescription` covers `confirmBuyerTaskComplete`/`confirmBuyerTaskUpdate` (adds a `NOT_NEEDED` case, since `HomeBuyerTaskStatus` has one and `MaintenanceTaskStatus` doesn't), and a new `buyerFindingConflictDescription` covers `confirmBuyerFindingDisposition`, naming the finding's current `buyerDisposition` via a shared `BUYER_FINDING_DISPOSITION_LABELS` map — also reused for the handler's own success-path `dispositionLabel`, so the conflict message and the eventual success message describe a disposition the same way rather than two independently-maintained label sets. `confirmBuyerTaskUpdate`'s combined `!task || version-mismatch` check now falls back to a generic "no longer available" message only when the row is genuinely gone, using the dynamic description whenever the row still exists — mirroring the exact ternary `confirmMaintenanceTaskUpdate`'s own race-recovery path already uses (`raceCurrent ? maintenanceConflictDescription(raceCurrent) : 'This task is no longer available...'`).

**Verification: 7 new unit tests** in `tests/ask/buyerConflictDescription.test.js` (completed/cancelled/not-needed tasks each name that explicitly; an in-progress task with a due date interpolates its real status/priority/date; a blocked task with no due date discloses "unscheduled" rather than fabricating one; a finding names its current disposition; a finding with no subsystem and an unrecognized disposition both degrade gracefully rather than throwing). All pass, alongside a clean `tsc --noEmit` and fresh-parse check. Not independently tested at the confirm-handler call-site level (composes a live Prisma lookup) — same STATIC-verification boundary applied throughout this audit series. **Not** database- or browser-verified — the actual rendered conflict card on a genuine concurrent edit was not exercised in a live browser this session.

Scored PASS: the retry path genuinely re-reads and acts on current state internally (it was never fooled by stale data, even before this fix), and now the current state is actually *shown* to the homeowner too, closing the gap this scenario specifically tests. "Contract revision" itself still has no directly testable write path in the current operation family, so this verdict remains scoped to the closest analogue, not a literal contract-edit flow — a scope note, not a remaining gap in what was actually fixed.

## B07 — Open a buyer workspace and return

**Required:** "Plan, filter, selection and position restored; changed data revalidated."

**Original verdict: FAIL.**

Confirmed by direct grep: there is no Buyer-equivalent of `MaintenanceViewState` (`askOrchestrator.service.ts:1520`) — no `resultId`, no stored filter/selection/position, no lookup keyed off `launchContext.sourceExecutionId` for any Buyer read. `MAINTENANCE_STATUS` is the one operation in the entire 77-operation registry with this mechanism (Phase 0 §4.15's own finding). Several Buyer operations do carry real, targeted outbound hrefs (`?taskId=` on `BUYER_PLAN_STATUS`/`BUYER_DEADLINES`/`BUYER_TASK_UPDATE`/`BUYER_COST_READINESS`, `?filter=MOVE` on `BUYER_MOVE_STATUS`) — a homeowner can navigate *to* the right place — but nothing restores the Ask conversation's own prior filter/selection/scroll position when they come back, and there is no revalidation-on-return mechanism distinct from just re-asking the question fresh.

**Correction, same session, 2026-09-17: this FAIL overstated the gap — a direct trace of the frontend (`AskWorkspace.tsx`/`resultViewState.ts`/`useResultView.ts`) found most of the mechanism is already generic, not Maintenance-exclusive as originally characterized:**
- **Scroll-position restore already works for Buyer, unmodified.** `useResultView`'s storage key falls back to the raw `executionId` when no `viewState` exists (`resultViewKey(sessionId, propertyId, execution.viewState?.resultId ?? execution.executionId)`), and `restoreResultPosition` (`AskWorkspace.tsx:2014`) is called generically for whatever execution the homeowner returns to — not gated to Maintenance.
- **Data revalidation on return already works for Buyer, unmodified.** The same return-navigation flow calls `refreshResult(target)` (`AskWorkspace.tsx:1809`) unconditionally, for any operation.
- **Filter restore is genuinely absent, but moot for Buyer as of this session** — Buyer has no filters at all yet (every read declares `filters: []`, Phase 0 §4.10); building filters is B02's gap, not this one's. Since the FRD's requirement is literal ("plan, filter, selection and position restored"), a missing filter mechanism keeps this scenario short of a full PASS regardless of what else is fixed.
- **Selection restore was the one genuinely broken piece**, for two independent reasons: (1) `reconcileResultView` (`resultViewState.ts:36`) hardcoded `block.id === 'maintenance-groups'` when computing which item ids are still valid — for any other `GROUPED_LIST` block, that set was always empty, silently nulling `selectedTaskId` on every hydration regardless of whether the item was still there; (2) even with a correct id set, the generic `GROUPED_LIST` item renderer never set `data-ask-task-id` or applied a selected-row highlight — both existed only inside the Maintenance-only `MaintenanceResultList.tsx` component, so a correctly-preserved selection still had no visible effect.

**FIXED, same session, 2026-09-17.** `reconcileResultView` now collects valid item ids from every `GROUPED_LIST` block in the result, not just one named block id — selection/expanded-row reconciliation is otherwise unchanged: an item that's still present survives, one that's gone clears to `null` (never substituted). The generic item renderer (`BlockView`'s `GROUPED_LIST` branch, `AskWorkspace.tsx`) now reads `ResultViewContext` and sets `data-ask-task-id={item.id}` + `tabIndex={-1}` + the same `border-teal-600 bg-teal-50` selected-row style `MaintenanceResultList` already uses — read-only, since this generic renderer has no "Select task" control of its own; it only reflects selection state already captured generically from an outbound `?taskId=` link click (the article's existing `onClickCapture` handler, unchanged).

**Verification: 7 new tests** — 4 in `apps/frontend/src/features/ask/__tests__/resultViewState.test.ts` (a selected item on a non-Maintenance `GROUPED_LIST` block survives reconciliation; a removed item clears selection rather than substituting; `expandedRows` reconciles the same way; a non-`GROUPED_LIST` block contributes no ids) and 3 added to `apps/frontend/src/components/ask/__tests__/resultContinuity.test.tsx` (a selected generic row receives the `data-ask-task-id` marker and highlight class, and an unselected sibling row doesn't; Buyer selection survives a same-execution refresh; a Buyer task leaving the result clears selection rather than substituting). All 14 tests in the `ask` feature area pass, alongside a clean `tsc --noEmit` and the full frontend suite (17 pre-existing, unrelated failures confirmed present before this change too, via `git stash` — none newly introduced). `BlockView` was exported (previously module-private) so it could be tested directly, same convention as `MaintenanceResultList`'s own export. **Not** browser-verified — no live click-navigate-away-and-return round trip was exercised in an actual browser this session.

**Interim verdict: `PARTIAL`.** Plan (property/session scoping), selection, position, and revalidation-on-return all now work for Buyer's generic `GROUPED_LIST` results — but filter restoration remained blocked on B02 (no Buyer filter mechanism existed at all yet), so the scenario's literal, full requirement ("plan, filter, selection **and** position restored") was not met end to end.

**Filter-restoration verification, same session, 2026-09-17, per an explicit user instruction to verify (not build new filters) now that B02 gave `BUYER_DEADLINES` real ones:** the viewState-construction logic inside `buyerDeadlinesResult` was extracted into a new pure, exported `buildBuyerDeadlinesViewState(priorViewState, laneFilter)` (`askOrchestrator.service.ts`, mirroring `selectBuyerDeadlineMilestones`'s own extraction — the function itself still requires a live `loadBuyerPlanContext` DB call, so this is the smallest slice that's directly testable without mocking Prisma) — no behavior change, same 6-line construction as before. 5 new unit tests in `tests/ask/buyerDeadlineLaneFilter.test.js` prove the round-trip genuinely holds: a fresh query (no prior state) mints a `resultId` and starts at `revision: 1`; re-issuing the same lane-filtered message on a simulated refresh (same `priorViewState`, same parsed lane) reproduces the identical `resultId` and `statusFilter` with `revision` incremented by exactly 1; a `selectedTaskId` survives a lane-filtered refresh untouched; switching lanes on a later turn keeps `resultId` stable while `statusFilter` updates to the new lane; clearing back to "All" resets `statusFilter` without minting a new `resultId`. This confirms the mechanism this scenario needs — `BUYER_DEADLINES` was already added to `FILTER_CONTINUABLE_OPERATIONS` and given a real `AskViewState` during B02 — actually round-trips, not just that it was wired up.

**Revised verdict: `PASS`.** Plan, filter, selection, and position are now all confirmed restored for Buyer's one operation with a real filter mechanism (`BUYER_DEADLINES`); revalidation-on-return already worked. The scenario's literal, full requirement is met for the operation that has filters. Scored PASS rather than a hedged PARTIAL because the FRD's B07 text names no specific operation — it requires the mechanism to work where filters exist, and for `BUYER_DEADLINES` it now demonstrably does, closing the gap this scenario's PARTIAL was tracking. `tsc --noEmit` clean, fresh-parse clean. **Not** browser-verified — no live click-navigate-away-and-return round trip was exercised in an actual browser this session.

## B08 — Required source document is missing

**Required:** "Honest missing-context state and supported document/capture action."

**Verdict: PASS.**

Confirmed across multiple independent Buyer operations, not just one: `buyerFinancingReadinessResult`'s `UNKNOWN`/no-readiness branch ("Purchase financing has not been recorded yet... select a confirmed Loan Estimate"), `buyerTitleEscrowReadinessResult`'s no-workspace branch, `buyerWalkthroughReadinessResult`'s not-scheduled branch, `buyerDisclosureFundsReadinessResult`'s no-Closing-Disclosure branch, and `buyerContractTimelineResult`'s no-confirmed-revision branch all return an honest `READY_WITH_LIMITATIONS`/`NEEDS_CONTEXT` state with a specific action pointing at where to add the missing record — none of them fabricate readiness or silently treat the missing record as "not applicable."

## B09 — User lacks permission for financial detail

**Required:** "No leakage; read/action surface reflects current role."

**Verdict: PASS, with an interpretive note.**

*Action surface*: confirmed strict. All 5 Buyer command operations (`TASK_COMPLETE`/`CREATE`/`UPDATE`, `FINDING_DISPOSITION`, `LIFECYCLE_UPDATE`) check `access.role === HouseholdRole.VIEWER` and return `BLOCKED` **before any data lookup**, at both propose- and confirm-time independently.

*Read surface*: `buyerFinancingReadinessResult`'s data source, `BuyerPurchaseLenderReadinessService.get`, asserts only a `VIEWER` minimum (`buyerPurchaseLenderReadiness.service.ts:90`) — a Viewer sees the same lender-condition detail a Contributor or Owner would. This is not a leak in the sense of bypassing a declared boundary: `BUYER_FINANCING_READINESS`'s own registry entry declares `VIEWER` as its role floor, and this is the *uniform* pattern across all 13 Buyer read operations (every one is `VIEWER`-floor; every one of the 5 commands is `CONTRIBUTOR`-floor) — a deliberate, consistent "Viewer reads everything, Contributor+ writes" model, not something forgotten specifically for financial data. Read as "current role" meaning the property-level household role (not a separate financial-sensitivity tier), the requirement is met. Flagged as an interpretive call, not a certainty, since BUY-010's phrase "sensitive financing... follows existing access policy" could also be read as calling for a stricter tier that doesn't exist in the code.

## B10 — No relevant next step exists

**Required:** "Plan response completes without generic suggestions."

**Original verdict: FAIL.**

Confirmed by direct reading of the terminal return statements: `buyerPlanStatusResult` always returned `suggestions: ['What is due before closing?', 'Which transaction documents are missing?']` regardless of whether `overview.nextAction` was present and regardless of blocker count — including the case where there was genuinely no open task and no blocker. `buyerCostReadinessResult` always returned the same 2 suggestions whether or not any costed tasks existed. This pattern held across every Buyer read checked — suggestions were a static pair keyed to the *operation*, not conditionally derived from whether a relevant next step actually existed for *that* response. ROLL-009's "the valid outcome of no suggestion" was not implemented anywhere in the Buyer family.

**FIXED, same session, 2026-09-17: `PASS`, scoped to the 2 operations this finding named concretely.** `buyerPlanStatusResult` now suppresses suggestions entirely when there is genuinely nothing open (`!overview.nextAction && !overview.blockers.length`) — the same condition its own SUMMARY block already used to render "No open next task is currently recorded." `buyerCostReadinessResult` now suppresses suggestions when zero costed tasks exist, since the SUMMARY block's own "Add an estimated cost to a Buyer Plan task..." CTA already covers "what to do next" in that case — a chat-level suggestion had nothing genuinely relevant left to add. Deliberately scoped to exactly these 2 operations (the ones this finding traced with concrete evidence) rather than a platform-wide sweep across all 13 Buyer reads the finding's own prose gestured at but didn't individually verify — extending this to the rest of the Buyer family, or other tracks, would need its own explicit request.

**Verification:** covered by the `tsc --noEmit`/fresh-parse check above (the suggestion-suppression conditionals themselves are simple, inline boolean checks against data already read in each function — not independently extracted, since wrapping a single boolean expression in its own function would be over-engineering for what it is). **Not** database- or browser-verified.

## Phase 6 exit criterion, second clause: "no undeclared item mutation"

**Verdict: PASS.**

All 5 Buyer command operations are registered in `askDomainCommandRegistry.ts` (confirmed in Phase 0 Stage 1) and dispatch exclusively through the canonical `confirmCapabilityInvoke`/`confirmAskExecution` path — there is no ad hoc write, no ability for presentation metadata to trigger a mutation outside the registered command set (XACT-001, verified platform-wide across all 77 operations during Phase 0 Stage 2, not assumed for Buyer specifically). Nothing found during this verification pass or the underlying Phase 0 tracing contradicts this.

## Summary

| ID | Required outcome | Verdict |
| --- | --- | --- |
| B01 | Buyer-plan status: phase, blockers, counts, freshness | **PASS** (fixed) — new `buyerJourneyStageLabel` surfaces `overview.journey.stage` in the SUMMARY body ("You're in the ... phase") |
| B02 | Filter deadlines by phase | **PASS** (fixed) — 4-lane filtering + real `AskViewState` continuity for blockers via `overview.blockersByLane` (pre-cap, authoritative counts) and for milestones via a new canonical `BUYER_MILESTONE_TYPE_LANE` mapping + `overview.milestonesByLane`/`unmappedMilestones` |
| B03 | Complete a task: review, one write, receipt, reconciliation | **PASS** (fixed) — reuses B04's `ASK_MUTATION_IMPACT_MAP`/`reconcileAskExecutionSideEffects` mechanism; `BUYER_TASK_COMPLETE` added to the impact map with the same siblings |
| B04 | Reschedule discloses old/new date, refreshes affected views | **PASS** (2 rounds of fixes) — round 1: current due date + real `editableFields`; round 2: server-owned `ASK_MUTATION_IMPACT_MAP` refreshes `BUYER_PLAN_STATUS`/`BUYER_DEADLINES`/conditionally `BUYER_MOVE_STATUS`, capped to `childExecutions`' max of 3, plus a platform-wide confirm-replay fix so a retried confirm still redelivers reconciliation; 11 new unit tests |
| B05 | Ambiguous finding → target clarification | **PASS** |
| B06 | Stale confirmation blocked, current state shown | **PASS** (fixed) — new `buyerTaskConflictDescription`/`buyerFindingConflictDescription` (mirroring `maintenanceConflictDescription`'s shape) now interpolate real current-record state into all 3 Buyer conflict messages |
| B07 | Workspace round-trip restores plan/filter/selection/position | **PASS** (fixed) — scroll/revalidation already worked generically; selection now genuinely restored (`reconcileResultView` generalized + generic renderer highlight added); filter restoration verified round-trips for `BUYER_DEADLINES` via a new `buildBuyerDeadlinesViewState` |
| B08 | Missing source document handled honestly | **PASS** |
| B09 | Role-appropriate read/action surface | **PASS** (interpretive) |
| B10 | No forced suggestions when nothing is relevant | **PASS** (fixed, scoped to the 2 operations named concretely) — `buyerPlanStatusResult`/`buyerCostReadinessResult` now suppress suggestions when genuinely nothing is open/costed |
| — | No undeclared item mutation | **PASS** |

**10 full passes, 0 partial passes, 0 fails, out of 10 scenarios.** Originally scored 4/2/4; external review corrected B06 (PASS→PARTIAL); the B04 round-1 fix corrected B04 (FAIL→PASS); a second external review then re-scored B04 (PASS→PARTIAL) for a reconciliation-scope overclaim; a round-2 B04 fix (server-owned mutation-impact map, per an explicit scoped design decision) closes that gap too; B03 then reused the same mechanism (`BUYER_TASK_COMPLETE` added to `ASK_MUTATION_IMPACT_MAP`); B07 was then re-traced and found far narrower than originally characterized — 3 of its 4 sub-requirements (plan, selection, position, revalidation) already worked generically or now do after a small, precisely-scoped frontend fix, with filter restoration noted as blocked on B02 at the time; B02 itself was then built (4-lane filtering + `AskViewState` continuity for blockers, 2 rounds of user correction during scoping), closing the "no filter mechanism at all" gap for `BUYER_DEADLINES` specifically, though milestones were left deliberately unfiltered pending a canonical phase mapping; B01 and B10 were then both fixed together (a new `buyerJourneyStageLabel` surfaces the current phase; both operations now suppress suggestions when genuinely nothing is relevant); B06 was then fixed — 2 new dynamic conflict-description functions, mirroring Maintenance's own reference implementation exactly, close the "static conflict message" gap this scenario specifically tests. Finally, B02's milestone-filtering gap and B07's filter-restoration gap were closed together in the same round, per explicit user approval of a canonical, exhaustive `BUYER_MILESTONE_TYPE_LANE` mapping (B02, PARTIAL→PASS) and an explicit instruction to verify — not build new filters for — `BUYER_DEADLINES`'s own mechanism, proven via 5 new round-trip unit tests against an extracted `buildBuyerDeadlinesViewState` (B07, PARTIAL→PASS) — netting to 10/0/0. The FRD's own Phase 6 exit criterion ("B01–B10 pass") **is now met** as of this verification, and the mutation-discipline clause continues to pass separately.
