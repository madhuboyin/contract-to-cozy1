import type { AskOperationId } from '../../ask/askOperationRegistry';
import type { SkillDefinition } from '../skill.contract';
import { PROPERTY_IDENTITY_CONTEXT_PROVIDER } from '../context/propertyIdentityContext.contract';
import { PROPERTY_JOURNEY_CONTEXT_PROVIDER } from '../context/propertyJourneyContext.contract';

export const REPAIR_REPLACE_SKILL_OPERATIONS: AskOperationId[] = [
  'REPLACEMENT_GUIDANCE',
  'HVAC_DECISION_START',
  'HVAC_DECISION_CONTINUE',
  'HVAC_DECISION_SCENARIO',
  'HVAC_DECISION_ABANDON',
  'HVAC_PREFERENCE_SAVE',
  'HVAC_PREFERENCE_FORGET',
  'HVAC_DECISION_OUTCOME_REPORT',
  'HVAC_DECISION_OUTCOME_VIEW',
  'HVAC_DECISION_OUTCOME_UNLINK',
];

const REPAIR_REPLACE_ADAPTERS = [
  'inventory.replacement',
  'decision-platform.hvac.start',
  'decision-platform.hvac.continue',
  'decision-platform.hvac.scenario',
  'decision-platform.hvac.abandon',
  'decision-platform.hvac.preference.save',
  'decision-platform.hvac.preference.forget',
  'decision-platform.hvac.outcome.report',
  'decision-platform.hvac.outcome.view',
  'decision-platform.hvac.outcome.unlink',
].map((id) => ({ id, version: '1.0' }));

export const REPAIR_REPLACE_SKILL = Object.freeze({
  id: 'repair-replace',
  version: '1.0.0',
  domain: 'HOME_CARE',
  displayName: 'Repair or Replace',
  description: 'Evaluate whether to repair or replace a recorded home system and continue its governed decision journey.',
  homeownerJobs: ['DECIDE_WITH_CONFIDENCE', 'NAVIGATE_MAJOR_MOMENTS'],
  supportedGoals: [
    'analyze-repair-or-replace',
    'continue-repair-replace-decision',
    'compare-repair-replace-scenario',
    'manage-repair-replace-preferences',
    'record-repair-replace-outcome',
  ],
  aliases: ['repair or replace', 'fix or replace', 'replacement decision', 'system replacement decision'],
  operations: REPAIR_REPLACE_SKILL_OPERATIONS.map((operationId) => ({ operationId, version: '1.0', requiredContextProviders: [PROPERTY_IDENTITY_CONTEXT_PROVIDER], optionalContextProviders: [PROPERTY_JOURNEY_CONTEXT_PROVIDER] })),
  requiredContextProviders: [PROPERTY_IDENTITY_CONTEXT_PROVIDER],
  optionalContextProviders: [PROPERTY_JOURNEY_CONTEXT_PROVIDER],
  allowedAdapters: REPAIR_REPLACE_ADAPTERS,
  allowedExternalConnectors: [],
  consumerPolicy: [{ consumer: 'ASK', operations: REPAIR_REPLACE_SKILL_OPERATIONS }],
  autonomyLevel: 2,
  riskPolicy: {
    effects: ['READ', 'WRITE'],
    materiality: 'MATERIAL',
    riskDomains: ['HOME_SAFETY', 'FINANCIAL', 'PRIVACY'],
    reversibility: 'PARTIALLY_REVERSIBLE',
  },
  authorizationFloor: 'VIEWER',
  allowedResultBlocks: [
    'SUMMARY',
    'GROUPED_LIST',
    'TABLE',
    'EVIDENCE',
    'BOUNDARY',
    'CAPABILITY_LIST',
    'DECISION_PROGRESS',
    'WHY_NOW',
    'RECOMMENDATION_CHANGE',
    'PREFERENCE_REFERENCE',
    'LIMITATION',
    'ASSUMPTIONS',
    'EMPTY_STATE',
    'SCENARIO_COMPARISON',
    'WORKFLOW_PROGRESS',
    'OUTCOME_SUMMARY',
  ],
  dependencies: [
    { type: 'CONTEXT_PROVIDER', id: PROPERTY_IDENTITY_CONTEXT_PROVIDER.id, version: PROPERTY_IDENTITY_CONTEXT_PROVIDER.version, required: true },
    { type: 'CONTEXT_PROVIDER', id: PROPERTY_JOURNEY_CONTEXT_PROVIDER.id, version: PROPERTY_JOURNEY_CONTEXT_PROVIDER.version, required: false },
    ...REPAIR_REPLACE_SKILL_OPERATIONS.map((operationId) => ({
      type: 'OPERATION_CONTRACT' as const,
      id: operationId,
      version: '1.0',
      required: true,
    })),
    { type: 'CANONICAL_SERVICE_CAPABILITY', id: 'inventory-repair-replace-analysis', version: '1.0', required: true },
    { type: 'CANONICAL_SERVICE_CAPABILITY', id: 'decision-platform-hvac-repair-replace', version: '1.0', required: true },
  ],
  contextBudget: {
    maxFacts: 100,
    maxEntities: 50,
    maxDocuments: 5,
    maxHistoryEvents: 200,
    maxSerializedBytes: 128_000,
    maxProviderLatencyMs: 5_000,
    maxOverallLatencyMs: 15_000,
  },
  evaluationSuite: 'skill-repair-replace-golden',
  featureFlag: 'ASK_SKILL_REPAIR_REPLACE_ENABLED',
  killSwitch: 'ASK_SKILL_REPAIR_REPLACE_KILL_SWITCH',
  owner: 'Homeowner Product / Home Care / Decision Platform',
  lifecycleStatus: 'DEVELOPMENT',
  operationalStatus: 'ENABLED',
} satisfies SkillDefinition);
